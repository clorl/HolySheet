All routes in the (app) group can only be accessed by authenticated users.
Similarly, (guest) can only be accessed by non-authenticated users.
Everything else is public

The (site) routes have the navbar etc, the (app) routes don't
